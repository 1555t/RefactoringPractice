def calculate(x):
    total = 0
    for i in x:
        total += i
    return total / len(x)

def useless():
    print("why is this here")

def calcAgain(values):
    return sum(values) / len(values)
